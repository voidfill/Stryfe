import { private_channel } from "@renderer/constants/gatewaytypes";
import Store from ".";

function makePrivateStored(raw: private_channel):

export default new (class ChannelStore extends Store {
	constructor() {
		super({
			READY: () => {

			},
			CHANNEL_CREATE: () => {
		});
	}
})();
